# Provenance

New source-only fixture for the numerical constraint executable profile (design D2/D5). Its
predicate combines an admitted numerical comparison with a bare Boolean term. Numerical-claim
containment makes the unsupported Boolean branch an error, so generation must halt and name the
statement, location, construct, and corrective message. No extraction snapshot is committed
because a correct live capture halts before serialization.
