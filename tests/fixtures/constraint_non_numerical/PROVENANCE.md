# Provenance

New fixture for the numerical constraint executable profile (design D5/D7 and invariant I5).
It pairs one string-equality assertion, which must warn and remain catalog-visible without
executing, with one admitted numerical assertion, which must still lower and execute. The pair
proves warn-and-continue behavior rather than an isolated classification result.
