# Provenance

[AGENT] Item 1 public-live OD-A04 fixture authored from the approved design. The
two concrete `Cell` occurrences carry literal overrides `4.0` and `6.0` around
the `>= 5.0` predicate boundary.
