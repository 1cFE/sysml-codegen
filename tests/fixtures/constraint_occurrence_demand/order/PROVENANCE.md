# Provenance

[AGENT] Item 1 public-live ordering fixture authored from approved OD-A10.
Targets sort as `a_collision`, `b_nonliteral`, and `c_clean`; the public case
pins cross-route deduplication and deterministic target order. Collision and
nonliteral warning order are isolated in the corresponding kept unit case.
