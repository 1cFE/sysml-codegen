# Provenance

[AGENT] Item 1 public-live constraint-only fixture authored from approved OD-A09.
The only demand for `DemandConstraintOnly__plant__source__value` is the
constraint actual, and the literal is `23.0`.
