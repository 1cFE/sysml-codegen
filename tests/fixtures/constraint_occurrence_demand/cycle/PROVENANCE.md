# Provenance

[AGENT] Item 1 public-live R-5 fixture authored from approved OD-A05. `Node`
declares a finite branch before a self-recursive branch so an empty-subtree cycle
implementation can return a plausible finite prefix.
