# Status Report — After Self-Binding Replacement

**Recorded:** 2026-08-16 17:33 PDT
**Question:** Where will the project be when `self-binding-replacement` is complete?

## Bottom line

The exact instance-graph architecture is already the only code-generation front end. Completing
`self-binding-replacement` will make its customer-facing modeling rule usable end to end: both
validators reject the silently inert `in R = R` form, published guidance teaches the right form for
each ownership situation, both Fusion Tea model trees use the corrected form, and public live and
snapshot artifacts prove off-default values reach every and only their intended consumers.

That is not the end of ELABORATE-FIRST Item 8. The work item explicitly excludes the remaining
downstream package/study regeneration, July IFE impact audit, certification and reference-doc repair,
and the composed model-to-package-to-study proof. Stellarator receives one read-only triage run, not
a migration.

## Current position

- ELABORATE-FIRST Item 7 cut the product over to elaborate-then-project and deleted the legacy
  string-resolution front end. The merged codegen landing is `385e163`; remote `main` reaches the
  follow-up record commit `9ce5548`.
- Exact owner anchoring is certified locally. The behavior repair is `98970c9`; the evidence repair
  and audit follow it. This is the shipped-intent basis of product promise P-002.
- `self-binding-replacement` has completed Phases 1 and 2 of 5. Codegen commits `00825a1` and
  `816d35e` make failures legible, pin both owner classes, and provide a no-write-on-hazard migration
  tool. Agentic-mbse commit `6e7bf5c` fixes its validator in an isolated worktree.
- The decisive work remains: Phase 3 guidance and drift contracts; Phase 4 the twelve-file,
  two-tree Fusion Tea migration plus live/snapshot and mutation proofs; Phase 5 Stellarator triage,
  cross-repo regression checks, and local delivery commits.
- Codegen `main` is 34 commits ahead of `origin/main`. The plan authorizes local commits only and
  forbids pushes or pull requests. Completion therefore means a locally integrated and certified
  candidate across three repositories, not remote shipment.
- The current worktree contains the staged anchoring archival beside modified self-binding records
  and an untracked self-binding plan. This is known concurrent-session state, but it makes
  path-scoped commits a delivery requirement; the Phase 2 record already documents one near-miss.

## Position after this work item

What will be true:

- The architectural cutover and the authoring contract agree. A bad self-binding refuses before
  generation instead of producing a confident wrong result.
- Humans, project templates, and the agent-facing SysML skill receive one situational rule: rename
  the input for an owning-part value, name the occurrence path for another part, and describe exact
  owner qualification only according to its measured support.
- Both synchronized Fusion Tea source trees carry a mechanically bounded rename-only migration.
- Fusion Tea generates and seals with zero readiness diagnostics from live models and snapshot v6,
  with byte-identical packages across those routes.
- Two public mutations, including a nested occurrence, prove source-to-consumer propagation. This
  converts the Fusion Tea result from “generation succeeds” into evidence for P-001's design-search
  premise.
- Stellarator's exact-route failure is counted, named, and filed without changing its held model.

What will still be open:

- The rest of ELABORATE-FIRST Item 8: package and contract regeneration, duplicate-workaround
  removal, study lineage updates, stock-API TEAx execution, the July IFE impact audit, certification
  matrix and reference-doc repair, and one composed closing proof.
- Epic audit, close, and shipment after that remainder. The current work item creates local commits
  only.
- P-001 remains directional and partly built. The toolchain is causal, not study-selectable/acausal.
  The filed capability gap remains `[ACAUSAL-RELATIONS-CAPABILITY]`.
- A useful CATF design-search campaign is still blocked by the P1 cryogenic heat-leak coefficient
  defect. Calculation-definition gates are also a P1, 7–9 day implementation, but require a fresh
  owner authorization.
- The arrayed-owner spelling has a P3 diagnostic follow-up. The deep-literal-override lane remains
  outside P-002's measured evidence bound.

## Open epics and tracking health

### ELABORATE-FIRST

This is the real active P0 epic. Items 2–7 are complete; Item 8 is the remaining delivery area.
`self-binding-replacement` covers Item 8's restated modeling-guidance obligation and the source-model
migration half of downstream remediation. The downstream remainder still needs its declared home at
`.project/active/elaborator-downstream/`.

The epic record is stale in several places: its header still says `Ready`; Item 1 boxes remain open;
Item 3 still says the owner checkpoint is pending even though the same section records `GO`; and the
epic-level criteria have not been reconciled against completed Items 2–7. These are tracking defects,
not evidence that the cutover did not happen.

### GAP-CLOSE

This is an administrative open shell, not a competing product direction. The epic file still says
external F1 normalization is open, while `BACKLOG.md` records that leg closed in merged TEAx main.
The old PR wave also merged through the later lifecycle work. What remains is certification and
closure bookkeeping: reconcile the final-suite claim and stale checkbox/prose, then archive the
epic. It should not remain presented as an active P0 engineering stream.

### Backlog and active-folder hygiene

- `BACKLOG.md` says it was last updated 2026-08-13. It omits ELABORATE-FIRST from the P0 table even
  though SOURCE-IDENTITY points to it as the replacement epic.
- Its `In Progress` table still names February work (`generation-boundary`, `hierarchical-output`,
  `new-pipeline-explainer`) and does not name the current item.
- `.project/active/` contains forty item directories, many with completed audits or superseded work,
  despite the folder contract saying completed items move to `completed/`.
- `CURRENT_WORK.md` is the most current narrative, but it has accumulated extensive historical
  material. The top block and current self-binding entries are reliable; the folder and backlog
  indexes need a focused reconciliation pass.

## Recommended sequence

1. Finish `self-binding-replacement` Phases 3–5 and audit it independently.
2. Create and execute the bounded `elaborator-downstream` remainder instead of declaring Item 8 or
   ELABORATE-FIRST complete from the Fusion Tea source migration alone.
3. Audit and close ELABORATE-FIRST, then run the branch-level shipment gate. Separately close the
   stale GAP-CLOSE record and reconcile `BACKLOG.md` / `.project/active/`.
4. For the original design-search goal, fix the CATF cryogenic coefficient before a real CATF
   campaign, then run a composed demo item. Start calculation-definition gate implementation only
   if the owner authorizes that capability as the next product need.
