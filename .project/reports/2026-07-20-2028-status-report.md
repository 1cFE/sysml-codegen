# Status Report — Backlog Full-Context Walkthrough

> **Addendum (same day, after owner-directed verification and closes):**
> - `[GAP-CLOSE-F1-TEAX-NORMALIZATION]` is **CLOSED** — verified against merged teax main
>   `fa0e06a` (subagent verification; BACKLOG entry now carries the citations). GAP-CLOSE
>   Item 1 checked; the epic's remaining gap is a certification pass (Item 5 state + the
>   full-suite wave-gate leg), not new code.
> - `[CONSTRAINT-ARCH-UNIFY]` re-assessed against merged main `936315c`: the resolver-ladder
>   sub-scope was delivered by lifecycle Item 2; entry rewritten to the 4 residual sub-scopes
>   and re-graded P1 → P2.
> - PUSH-DOWN row fixed and closed: 4 child item dirs + the 4-doc epic family archived to
>   `completed/20260720_*`; CHANGELOG entry added.
> - Ten completed epic docs archived out of `backlog/` (PIPELINE-TRUTH, UPSTREAM-FINDINGS,
>   PUSH-DOWN ×4, OUTPUT-REGISTRY redesign, ATTR-EXPR, COST-PATTERN, EXPR-CODEGEN); CHANGELOG
>   entries added for those that lacked one. `epic_gap_close.md` + its independent audit stay
>   (epic still open). Flags 1–3 below are therefore resolved; flags 4–5 remain.

**Date:** 2026-07-20 20:28 (post-CONSTRAINT-LIFECYCLE close-out)
**Scope:** everything in `.project/backlog/` — BACKLOG.md tiers, the follow-on registers, and
the ten epic docs stored alongside it.

## The story arc (how the backlog got its shape)

Twelve epics in lineage order, each shaped by the previous one's residue:

1. **EXPR-CODEGEN** (Feb) — expressions compile to Python. Complete.
2. **ATTR-EXPR** (Feb) — computed attributes become synthetic modules. Complete.
3. **COST-PATTERN** (Feb) — hierarchy-aware codegen, conformance suite C01–C27. Complete.
4. **OUTPUT-REGISTRY/BACKTRACKER REDESIGN** — typed registries. Complete.
5. **UPSTREAM-FINDINGS** (07-06, PR #3) — fusion-tea findings + plant idioms. Complete.
6. **PIPELINE-TRUTH** (07-06) — "the generated package is the truth"; run-C bit-exact. Complete.
7. **TRUTH-DEBT** (07-08) — PIPELINE-TRUTH's deliberate deferrals retired. Complete, archived.
8. **PUSH-DOWN** (07-10, PRs #8/#10) — shared helpers to agentic-mbse. Certified/merged.
9. **CONSTRAINT-EXEC** (07-13) — modeled assertions execute; study layer. Complete, archived.
10. **GAP-CLOSE** (07-18) — constraint-expression gap closure. **Local scope certified; one
    external TEAx leg open** (the only genuinely open epic in backlog/).
11. **CONSTRAINT-WAVE** (07-19) — superseded mid-flight; archived at today's close.
12. **CONSTRAINT-LIFECYCLE** (07-20) — merged (#11→#9→teax#3) and archived today.

## What is genuinely OPEN (by priority)

**P0**
- `[GAP-CLOSE-F1-TEAX-NORMALIZATION]` — TEAx leg: attach failed module identity to normalized
  evaluator failures at the serial-executor seam. Blocks full GAP-CLOSE certification.

**P1**
- `[CONSTRAINT-ARCH-UNIFY]` `[AGENT]` — unified path/instance/resolution infrastructure
  (one resolver ladder, one part-structure index, shared live/offline phases). Filed from the
  code-quality review; owner ratified the filing.
- `[PROFILE-COMPILER-PARITY]`, `[INLINE-CONSTRAINT-WIRING]` `[AGENT]` — from the 2026-07-17
  remediation audit. **Need re-verification against merged main**: registered before the
  lifecycle epic ran and never marked absorbed by it (see Flags).
- PUSH-DOWN BACKLOG row — stale, see Flags (work is done; row says Ready).

**P2**
- `[NESTED-OCCURRENCE-OVERRIDE]` — nested `:>>` override lost (calc + constraint); probe
  fixture in-tree; owner scheduling decision pending.
- `[SYNC-F3]` (leaf-collision filename), `[SYNC-F4]` (override name surfacing) — both
  deliberately KEEP FILED until a real model hits them.
- `[CONSTRAINT-MODEL-INVARIANTS]`, `[CONTRACT-VERIFY-BOUNDARY]` `[AGENT]` — same 07-17 audit
  family; same re-verification caveat.
- `[V2-HTML-BUILD]` — build the v2 pipeline explainer HTML; owner-assigned to another agent;
  input brief re-anchoring is docs-lifecycle-sync Phase 4.

**P3 (live)**
- `[DM08-MODEL-FIELD-TYPING]` — NewType-annotate resolution-model name fields (typing churn).
- `[TRUTH-DEBT-INHERITED-FORMULA-COMPILE]` — inherited-attr FORMULAs classify right but build
  no module; thread ancestor attrs into compiler input_names.
- `[TRUTH-DEBT-IFE-PLANT-CHAIN-STALE]` — ife_plant snapshot latently stale (pre-existing
  classifier drift); member of the wider stale-baseline class flagged at epic close.
- `[DOCS-SCRUB-F3]` — four stale code docstrings (one-line fixes).
- `[SANITIZER-MERGE]`, `[SC11-IMPORT-REWRITE]`, `[DOTTED-LEAF-PART-BLIND]` — assessed and
  deliberately parked; each has a recorded "do only if/when" condition.
- `[ITEM5-SWEEP-RESIDUE-OVERFLOW]` — 32 matrix rows never line-by-line deep-read (honest
  budget-bound stop; exact row list recorded).
- `[DOC19-DISPATCH-REAUDIT]`, `[MODULEKIND-DOC-SWEEP]` — doc drift sweeps; candidates for
  absorption into docs-lifecycle-sync Phase 1.
- `[EXIT-PIN-SEAM]` — decide the dormant exit-selection API's fate.
- `[ANON-ELIGIBLE-KEY]` — anonymous executable assertions share one compile key; needs an
  owner ruling on whether the authoring form is supported.
- `[D3-HYGIENE-TAIL-SITE4-TRANSITIVE-ALIAS]` — cross-derivation membership check
  (design-level; the mechanical WARN would false-fire on 5 clean fixtures).

**Ideas (unprioritized, live)**
- InvocationExpression whitelist (sqrt/min/max); SelectExpression/if-then-else;
  EXPOSE_COMPUTED decomposition; non-uniform arrays; body-assignment capture (low value);
  `attribute :>>` extraction relaxation (validation-warning half built; relaxation deferred);
  constraint-reconstruction snapshot coverage; stale-fixture refresh chore (wi014_toy,
  self_named_binding_trap, quoted_owner_formula — overlaps the stale-baseline class).

## Resolved-but-retained entries (provenance, no action)

`[CONSTRAINT-SILENCE]`, `[TRUTH-DEBT]` ledger, `[DOCS-SCRUB-F1/F2/F4]`, `[ITEM7-F4-CUTOVER]`,
`[ITEM7-MATRIX-TEST-GAPS]`, `[ITEM7-CLASSIFIER-FIX]`, `[ITEM7-MATRIX-SWEEP-RESIDUE]`,
`[GB-PARAMGROUPS-TYPING]`, `[ITEM7-PGD06]`, `[SYNC-F5]`, `[CE-F1/F2/F3]`,
aggregation-literal dispatch idea. All carry RESOLVED/ABSORBED/RETIRED banners with the
absorbing item named.

## Flags (inconsistencies found this pass)

1. **PUSH-DOWN row is stale.** BACKLOG says "Ready (4 items, 6–8 days)" with all four items
   unchecked, but `epic_push_down.md` says ✅ Certified (2026-07-10) and the work merged as
   PRs #8/#10. Row and checkboxes need the completed treatment (strikethrough + archive), and
   the epic doc family (epic_push_down*.md, 4 files) is archive-eligible.
2. **The 2026-07-17 constraint-exec audit follow-ons predate the lifecycle epic** and were
   never dispositioned against it. Items 1–13 plausibly moved these surfaces (inline
   constraints, profile parity, seal verification). Before anyone picks one up, re-verify each
   against merged main `936315c` — some may be partially or fully overtaken.
3. **Epic-doc clutter in backlog/**: of ten epic docs, only `epic_gap_close.md` is genuinely
   open. Eight complete/certified epic docs (+ audit companions) are archive candidates:
   pipeline_truth, upstream_findings, push_down (×4), output_registry_backtracker_redesign,
   attribute_expression_capture, costed_component_pattern, expression_aware_codegen,
   gap_close_audit_independent.
4. **In Progress table is stale** (February entries): generation-boundary "BUILD phase",
   hierarchical-output draft, new-pipeline-explainer draft (the latter effectively superseded
   by `[V2-HTML-BUILD]` + deprecated Gen-1 explainer). Needs a truth pass.
5. **Doc-sweep overlap**: `[DOC19-DISPATCH-REAUDIT]` and `[MODULEKIND-DOC-SWEEP]` cover the
   same ground as the planned docs-lifecycle-sync Phase 1 sweep — absorb or cross-reference
   when that item is approved.

## Recommended next actions

- Approve/adjust the `docs-lifecycle-sync` plan and fold the two P3 doc sweeps into its
  Phase 1 register.
- Run a small backlog-truth pass: fix the PUSH-DOWN row, archive the eight closed epic docs,
  refresh the In Progress table.
- Disposition the four 07-17 audit follow-ons against merged main before treating their P1/P2
  grades as current.
- Owner decisions queue (unchanged from close-out): branch delivery/deletion, stale-baseline
  owner, `[NESTED-OCCURRENCE-OVERRIDE]` scheduling, Item-10 completeness ruling,
  `[ANON-ELIGIBLE-KEY]` ruling.
