# Project Status Report

**Date:** 2026-07-10
**Scope:** PUSH-DOWN, product backlog, active work, and live PR state

## Executive Summary

The functionality refresh is largely complete. UPSTREAM-FINDINGS, PIPELINE-TRUTH, and
TRUTH-DEBT are implemented, audited, and merged in sysml-codegen. The older product epics
COST-PATTERN, ATTR-EXPR, and EXPR-CODEGEN are also closed.

PUSH-DOWN is implemented and pre-PR tested, but it is not closed. An independent audit found
four fix-before-merge issues in Items 1 and 2. PR #8 in sysml-codegen and PR #10 in agentic-mbse
are open and review-blocked. Items 3 and 4 certify as implemented.

The backlog index is stale. It labels PUSH-DOWN as Ready with four unchecked items, labels the
implemented explainer as Draft, and leaves many completed item folders under `active/`.

## Fully Closed

- UPSTREAM-FINDINGS: all 12 items audited and merged as sysml-codegen PR #3.
- PIPELINE-TRUTH: all 10 items implemented and audited; sysml-codegen landed through PR #5.
- TRUTH-DEBT: all six items implemented, audited, archived, and landed through PR #6.
- COST-PATTERN, ATTR-EXPR, and EXPR-CODEGEN: recorded in the completed backlog and changelog.
- PUSH-DOWN Items 3 and 4: implementation and item audits are complete, with no independent-audit
  corrections required. They remain part of the still-open epic PRs.
- PIPELINE-TRUTH's agentic-mbse dependencies: PR #7 and companion PR #8 are merged.

## Not Closed

### Immediate Release Work

1. Fix the four PUSH-DOWN independent-audit findings:
   - resolve the unperformed `BindingInfo` rename claim;
   - document or remove the widened Item 1 membership filter and mock-only branches;
   - add or correct the promised intentional-divergence marker;
   - address or explicitly amend the mypy 97-to-98 regression caused by missing `py.typed`.
2. Re-run focused and full gates, update the audit/certification record, then obtain review and
   merge sysml-codegen PR #8 and agentic-mbse PR #10.
3. Open and merge the fusion-tea workaround-retirement PR. No PR exists for branch
   `chore/retire-pipeline-truth-workarounds` as of this report.
4. Archive PUSH-DOWN artifacts and update the backlog/changelog only after the PRs merge.

### Active Product Work

- `generation-boundary` (P1): implementation phases 1, 2, and 4 and the BUILD gate are complete.
  Phase 3/5 cutover and cleanup, validation, documentation updates, and commit/closeout remain.
- `hierarchical-output` (P2): draft spec only. It needs design before implementation.
- `new-pipeline-explainer` (P2): code is substantially implemented and the plan says Complete,
  but browser, interaction, cross-browser, and comprehension validation remains unchecked. Treat
  this as validation/closeout work, not a draft feature.

### Filed Backlog Work

- Conditional P2 items: SYNC-F3 filename collision and SYNC-F4 name surfacing. Both are explicitly
  trigger-driven and should wait for a real consumer/model.
- Useful P3 correctness/capability items: inherited-FORMULA compilation, stale `ife_plant`
  recapture, two-level `attribute :>>` extraction, and AST-based import rewrite.
- P3 typing/quality work: DM08 model-field typing, docstring cleanup, sanitizer consolidation,
  dotted-leaf part awareness, constraint reconstruction coverage, and remaining matrix sweep.
- Several rows are already absorbed, resolved, retired, or discharged but remain in the live
  backlog. These should be moved to provenance/archive sections rather than treated as open work.

## Recommended Order

1. Fix and merge PUSH-DOWN. This is the only P1 release blocker and the changes are already scoped
   by the independent audit.
2. Open the fusion-tea workaround-retirement PR so the completed cross-repository refresh is fully
   landed.
3. Resume `generation-boundary`. It is the highest-priority unfinished product item and is already
   through its BUILD gate.
4. Validate and close the pipeline explainer. Most implementation is already present, so this is a
   bounded way to remove stale active work.
5. Reassess `hierarchical-output` against current consumer demand before designing it.
6. Batch backlog hygiene: mark PUSH-DOWN accurately, archive completed active folders, remove
   absorbed rows from the actionable list, and promote only trigger-backed P3 work.

## Record Inconsistencies

- `BACKLOG.md` says PUSH-DOWN is Ready and its four items are unchecked; the epic and item audits
  show implementation complete, while the independent audit says Needs Work before merge.
- `epic_push_down.md` still says Certified and its next action says run pre-PR, although pre-PR ran
  and the PRs are open.
- `CURRENT_WORK.md` contains the correct independent-audit warning but also retains superseded
  certification and stale PIPELINE-TRUTH human actions.
- `new-pipeline-explainer` is Draft in the backlog, Complete in its plan, and incomplete in its
  validation checklists.
- Most PIPELINE-TRUTH and PUSH-DOWN item directories remain under `active/` after audit completion.

