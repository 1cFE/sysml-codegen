# The final candidate — the corrected tree, its three identical gate runs, and what remains for the owner

**Assembled 2026-08-14** (narrow-correction step 8) by `phase5-runs/build_candidate_final.py`
from the step-7 battery under `phase5-runs/final-runs/` and the tree itself. Status:
**ASSEMBLED at the content OIDs — step-9 fresh narrow audit and owner final disposition
PENDING.** This record supersedes the gate-2 post-REVISE record (content `6c35aa0`/`3fbda2f`),
which the 2026-08-12 narrow-correction verdict kept as evidence but retired as an acceptance
candidate; the `013d6a1` revision of this path holds the still-older gate-1 record.

**The tree-final ruling.** **[OWNER 2026-08-14]** ruled the tree final ("no changes in
flight") at codegen `540ad59`. The battery's own replacements checker then caught one stale
ledger citation (L-179 — a step-5 rename miss, §6), repaired in the bookkeeping commit
`2819501`. The shipped-path diff between the ruled OID and the content OID is **empty**,
asserted by the builder at assembly time (`candidate.json`,
`shipped_diff_vs_owner_ruled_oid`). All three battery runs measured the repaired tree.

Every number in this record is derived — run values parsed from the committed battery logs,
repository facts from Git, hashes from bytes — by the builder; the tables below are quoted
verbatim from its output (`phase5-runs/final-candidate-tables.md`). Nothing is typed in by
hand. §7 records how to re-derive everything; §8 records what this record does not claim.

---

## 1. The candidate

The candidate is the **corrected retired tree**: the exact instance-graph route as the only
generation authority, the legacy string-resolution stack gone (T7's four retirement commits,
no provisional trim), and the six ratified narrow-correction steps executed on top (T2). The
step-by-step completion records live in `plan.md` under "Narrow correction — executable
sequence"; the ratified dispositions in `owner-disposition-20260811.md`.

## T1 — The candidate

| | sysml-codegen | agentic-mbse |
|---|---|---|
| Path | `/home/reid/1cfe/sysml-codegen-item7-rebuild` | `/home/reid/1cfe/agentic-mbse-item7-rebuild` |
| Branch | `item7-rebuild` | `item7-rebuild` |
| Content OID | `2819501178370db230acefdbcd02dfa15b409ac4` | `6372ef7ba6ba4c869759fcf201c59aa128175c6f` |
| Item 6 base | `1672c5766f67e7716f3c9f8f636c21e2ea444601` | `5088b417c9e5453271291d46cd5fb23fc0579b1e` |
| Diff vs base | 1176 files changed, 295589 insertions(+), 106678 deletions(-) | 36 files changed, 1221 insertions(+), 374 deletions(-) |
| Name-status vs base | 825 added / 137 modified / 18 renamed / **196 deleted** | 6 added / 30 modified / 0 renamed / **0 deleted** |
| Commits since base | 370 (`1672c5766f67e7716f3c9f8f636c21e2ea444601..2819501178370db230acefdbcd02dfa15b409ac4`) | 9 (`5088b417c9e5453271291d46cd5fb23fc0579b1e..6372ef7ba6ba4c869759fcf201c59aa128175c6f`) |
| Shipped paths (`src`, `tests`, `scripts`, `docs`, `pyproject.toml`) | clean | clean |

TEAx (evidence-only): `75eecb3bcf4baa0306107a96aa78b74ee667e970` on `constraint-semantics-item3` — moved from the Item-3-close pin fa0e06a9-descendant 5b70ae9 by docs-only commits of the closed CONSTRAINT-SEMANTICS epic (verified: one file, docs/evaluation-and-study.md); no simkit code changed.

## T2 — The narrow-correction step commits

| step | commit | subject |
|---|---|---|
| 1 | `f424d7e` | refactor(item7): one catalog authority, and the dead extraction surfaces dispositioned |
| 1 | `009a67a` | docs(item7): re-cite the verification matrix against the retired tree |
| 2 | `057bf29` | Complete exact compiler convergence |
| 3 | `8aa3b28` | fix elaboration same-leaf rollup names |
| 4 | `cc268d5` | brief(Item 7): step-4 brief revised to rev 2 at phase-B resumption — register walked and routed, REQ-CL-03 ruled (usage domain is the totality boundary) |
| 4 | `7ebe447` | step4(Item 7): matrix replacement coverage closed — 0 UNTESTED, REQ-CS family minted, gain=100 three-route proof, smart-regen subset cure |
| 5 | `a4669af` | step5(Item 7): portable live provenance — routes render one referent, generated bytes equal |
| 6 | `540ad59` | step6(Item 7): R12 amended to the zero-new ruff baseline — measured sets recorded, SC box honestly held |

The commits between these on the line are their paired docs anchors and the CONSTRAINT-SEMANTICS
epic executed mid-sequence during the recorded pause (plan.md, "PAUSED at step 4"); the
`2819501` repair commit (§6) sits on top of step 6.

## T3 — Path inventory

| | sysml-codegen | agentic-mbse |
|---|---:|---:|
| Tracked files | 2,363 | 1,840 |
| Production modules (`src/**.py`) | 72 | — |
| Test modules | 158 | 68 |
| Fixture directories | 123 | — |
| Numbered reference documents | 31 | — |
| Scripts (`scripts/**.py`) | 45 | — |

---

## 2. Environment

Asserted **before** any measurement in each of the three runs, and each run aborts if the gate
fails.

```
import-path gate  PASS   (all three runs)
python            3.12.11  /home/reid/1cfe/item7-rebuild-venv/bin/python
sysml_codegen     /home/reid/1cfe/sysml-codegen-item7-rebuild/src/sysml_codegen/__init__.py
agentic_mbse      /home/reid/1cfe/agentic-mbse-item7-rebuild/src/agentic_mbse/__init__.py
simkit            /home/reid/1cfe/teax/packages/teax-simkit/simkit/__init__.py
ruff 0.16.2   mypy 2.3.0   pytest 9.1.1   pydantic 2.13.4   jinja2 3.1.6   syside 0.8.4
license           key present (set -a; source /home/reid/1cfe/agentic-mbse/.env; set +a)
```

The gate pins each of the three imports to its own required worktree, so a cross-wired venv
cannot pass it. Neither protected original checkout was touched.

**The licence proof is direct, not inferential.** The codegen suite runs with `-rs`, so every
skip prints its reason, and `no live syside license` appears **zero** times in all three runs.
The 34 skips are model-content skips.

---

## 3. Three consecutive complete runs, at the content OIDs

Three complete batteries (`run_final_battery.sh` via `chain_final.sh`), each: environment
gate, both full suites with `-rs`, the execution lane on its canonical scope
(`tests/execution -m execution`), the corpus gate, `capture_v6_batch.py --verify/--check`
with a non-timestamp fixture-churn check, ruff and mypy in both repositories, the four
ledger checkers, proof integrity, doc distinctness, the scale protocol, and diff/status in
all three repositories. **Each run's `heads.tsv` records exactly the content OIDs in T1** —
unlike the gate-2 record, there is no separate head re-measurement, because the runs
measured the content tree itself.

## T4 — The three step-7 gate runs, at the content OIDs

| Gate | run 1 | run 2 | run 3 | identical |
|---|---|---|---|---|
| Full licensed codegen suite | 2086 passed, 34 skipped, 88 deselected | same | same | yes |
| `no live syside license` skip lines | 0 | same | same | yes |
| agentic-mbse suite, from the paired worktree | 1831 passed, 1 skipped, 5 deselected | same | same | yes |
| Execution lane (`-m execution`), incl. real TEAx | 88 passed | same | same | yes |
| Corpus ledger gate (`-k corpus`) | 9 passed, 2199 deselected | same | same | yes |
| `capture_v6_batch.py --verify` | 15 captured, 22 refused, 0 deviations | same | same | yes |
| Non-timestamp fixture diff after `--verify` | 0 | same | same | yes |
| `capture_v6_batch.py --check` | 15 captured, 22 refused, 0 deviations | same | same | yes |
| `ruff check src` | Found 12 error | same | same | yes |
| `ruff check src tests scripts` | Found 642 error | same | same | yes |
| `mypy src` | Found 52 errors in 11 files | same | same | yes |
| agentic `ruff check src` | Found 1 error | same | same | yes |
| agentic `ruff check tests` | Found 120 error | same | same | yes |
| agentic `mypy src` | Found 108 errors in 26 files | same | same | yes |
| `check_ledger_4a.py paths` | 304 rows checked, 0 problems | same | same | yes |
| `check_ledger_4a.py surface` | 0 unrowed breakages | same | same | yes |
| `check_ledger_4a.py replacements` — green | 223 | same | same | yes |
| `check_ledger_4a.py replacements` — not required | 79 | same | same | yes |
| `check_ledger_4a.py replacements` — FAIL | 0 | same | same | yes |
| `check_proof_integrity.py` | proof integrity: 0 problems over 0 blocked files | same | same | yes |
| `check_doc_distinctness.py` | 31 numbered reference documents checked, 0 identical-content groups | same | same | yes |
| `git diff --check`, codegen | rc=0 clean | same | same | yes |
| `git diff --check`, agentic | rc=0 clean | same | same | yes |

**51 / 51 compared fields identical across all three runs**, re-derived from the committed logs by this builder through `compare_final_runs.fields`. The rows above are the headline subset; the full field list is in `candidate.json` under `three_runs.fields`. Unlike the gate-2 record, the runs measured the content OIDs themselves, so there is no separate head re-measurement.

**The gate values against their governing clauses:**

- `ruff check src` **12** (codegen) and **1** (agentic) are byte-for-byte the enumerated
  zero-new baseline sets R12 carries since the step-6 amendment (disposition 2).
- `mypy src` **52 in 11** (codegen) is under the Item-6 baseline (71 in 17) with zero new.
- Both suite counts match the step-5 record's baselines exactly (codegen +0; the agentic
  count's provenance is the paired worktree at its own HEAD, which the CONSTRAINT-SEMANTICS
  epic moved from the REVISE-era 1825 to 1831).
- The **replacements checker swept 302 of the 304 ledger rows for the first time since the
  REVISE battery** and reads 223 green / 79 not-required / **0 FAIL** — the 223 is the
  REVISE-era 222 plus the repaired L-179 (§6). The two rows outside the sweep are the ledger's
  only `agentic-mbse` rows (L-036/L-037), which the checker structurally skips; their coverage
  was verified directly by the step-9 audit and re-verified at the disposition pass (65 passed),
  and their citations were re-pointed per audit-9 F1, `[OWNER 2026-08-14]` option (b).
  *(This sentence originally claimed a "full 304-row sweep"; corrected at the same pass.)*
- The R12-governed changed-tree ruff arm (`ruff check src tests`) reads **143** codegen /
  **121** agentic against baselines of 358 / 127 — passing with margin. T4's 642 is the
  battery's wider `src tests scripts` scope, which R12 does not govern (audit-9 F3).
- The execution lane's 88 include the `gain = 100` three-route mutation proof
  (`tests/execution/test_fusion_tea_mutation_teax.py`, visible in each run's
  `execution_lane.log`), discharging the Evidence-Invalidation Register row-3 single-shot
  obligation at the final paired OIDs — three times over, identically.

## T5 — Scale, from the same three runs

| fixture | live elaborate s | generate live s | peak RSS MiB (cumulative) | envelope bytes |
|---|---|---|---|---|
| `catf_mfe_d5` | 5.013–5.218 | 5.234–5.481 | 304.7–347.1 | 667,339 |
| `fusion_tea` | 0.125–0.140 | 0.201–0.221 | 222.2–244.1 | 112,707 |
| `solar_battery_d5` | 0.398–0.438 | 0.842–0.901 | 267.6–292.5 | 1,104,468 |

Envelope bytes and all deterministic counts are identity-compared across runs (they are part
of the 51 fields); wall-clock and RSS are reported as measured ranges, not identity fields.

---

## 4. The verification matrix

## T6 — Verification matrix (step-4 recount, standing)

**288 rows / 156 PASS / 1 PARTIAL / 131 RETIRED / 0 UNTESTED**, 34 families, 64 kept test files. Source: step-4 mechanical recount, plan.md 'Narrow-correction step 4 completion'; steps 5-6 edited zero rows.

The one PARTIAL (REQ-DIAG-01) is recorded deliberate. Zero UNTESTED is a step-4 first since
the retirement.

---

## 5. The retirement, executed

## T7 — The retirement commits (no provisional trim)

| commit | subject | paths touched |
|---|---|---|
| `19072ad` | retire(step 1): G2' — the v5 read path | 99 |
| `82c7951` | retire(step 2): the v5 family | 167 |
| `882fc8d` | retire(step 3): G3' — resolution/producer_completeness.py | 1 |
| `3071fba` | retire(step 4): G4' — the last reach at the legacy route, and the dead v5 surface | 4 |

`tests/conformance/test_public_authority_switch.py` and
`tests/unit/test_elaboration_import_boundaries.py` pin the absence of every retired module;
the T4 suites run them in each battery.

---

## 6. Surfaced at assembly — nothing resolved silently

1. **L-179 stale citation, caught by this battery and repaired.** The first battery pass
   FAILed ledger row L-179: it still cited
   `test_the_two_packages_differ_only_in_provenance_comments`, the pin step 5 renamed to
   `test_the_two_packages_are_byte_identical` when the route divergence died. Step 5
   re-pointed rows L-007/L-010 and missed this third citation; the full replacements sweep
   had not run since the REVISE battery, so it surfaced only here. Repaired in `2819501` via
   `scripts/_ledger_edit.py` (row green 10+1 passed; a full-ledger static sweep found no
   other stale citation); the pre-repair partial runs were discarded and all three recorded
   runs measure the repaired tree. Shipped-path diff vs the owner-ruled `540ad59`: **empty**,
   builder-asserted.
2. **The R12 mypy clause's agentic arm does not match any recorded measurement.** R12 says
   agentic mypy must "not exceed the Item-6 baseline of 105 errors in 23 files"; the
   measured value is **108 in 26** — identical in all three runs, identical to the REVISE
   battery, and the endpoint of a recorded **118 → 108** improvement during the recovery
   (plan.md Gate 3C-era record). The operative zero-new arms of the clause hold; the
   105-in-23 number appears in no battery log this recovery produced. Disposition 2 ratified
   amending the **ruff** clause only, so this record surfaces the discrepancy rather than
   amending the spec: it goes to the step-9 audit ("final gate semantics") and the owner.
   **Dispositioned:** audit-9 ruled the clause's number stale-by-environment (both Item-6
   bases re-measured under the mandated venv: codegen 73-in-18, agentic 118-in-28; the
   current agentic set is a strict subset — zero new, ten fixed), and `[OWNER 2026-08-14]`
   approved amending R12 to the measured values. The amendment is in the spec, dated.
3. **TEAx moved past the Item-3-close pin, docs-only.** T1's note: one file
   (`docs/evaluation-and-study.md`), from the closed CONSTRAINT-SEMANTICS epic; no simkit
   code. The lane imports simkit from this working tree; its 88 passed three times.
4. **Known, pre-existing, recorded elsewhere:** the lane's stub-runner tripwire fails under
   the broader-than-canonical `pytest tests/ -m execution` invocation (collection-order
   artifact, predates step 5; recorded in the step-5 completion record and CURRENT_WORK);
   the two stale-baseline classes (`deep_cross_scope`, `plant_values`) still need an owner.

---

## 7. How every number here was derived

`build_candidate_final.py` parses the committed battery logs through
`compare_final_runs.fields` — the same parser that produced `final-runs/comparison.md`
(51/51) — runs Git for repository facts, and hashes evidence bytes. `candidate.json` carries
the full field lists, the evidence SHA-256 map, and the builder-asserted shipped-diff check.
Re-running the builder at the record commit reproduces this record; re-running
`chain_final.sh` reproduces the battery. The commit-count definition is stated inline in T1
(`git rev-list --count` over the named range); there is one count and one definition.

## 8. What this record does not claim

- **No owner acceptance.** Step 10 is the owner's; this record authorizes no push, tag,
  promotion, close, or archive.
- **No audit verdict.** The step-9 fresh narrow audit runs after this record and reports in
  its own artifact; nothing here anticipates its findings.
- **No claim about the unmerged future**: merge sequencing (codegen before TEAx), the
  phase-D branch gate, and the worktree cleanup remain outside Item 7.
- **The mission-outcome and coordinated-gates SC ticks remain open** in the Item-7 spec:
  mission outcome is recorded owner-grade; coordinated gates now has its fresh exact counts
  in T4, with the §6.2 mypy-clause discrepancy surfaced for the audit and the owner rather
  than self-certified around.
