# Handoff — Item 7 Cutover Recovery: execute the owner's REVISE path

**Written:** 2026-08-11, end of the orchestrated recovery session (Phases 1–5 complete to the
owner stop; owner verdict: **REVISE**).
**Status legend:** every substantive claim below is marked **[V]** (verified this session by
direct measurement or by an independent audit re-run) or **[C]** (carried from a recorded
artifact, not re-checked at handoff time).

---

## Focus

Execute the owner's REVISE disposition on the Item 7 cutover recovery. The owner's review
(2026-08-11, reproduced in full in the session transcript and to be committed into the plan as
the disposition record) accepted the candidate as a credible **pre-retirement checkpoint** and
refused to call Item 7 complete. The prescribed path, verbatim in structure:

1. Record the 15/22 v6 batch as **accepted** (owner ruling, given).
2. Implement the seven gated migrations instead of holding them out (owner ruled items 1–2:
   coordinated exact-ID type migration in BOTH repos is Item 7 scope; items 3–5: replace or
   repoint the ~111/113 affected behavior tests before deleting their evidence sources; item 6:
   carry unknown-fixture rejection into the v6 capture driver; item 7: remove the dead v5
   exports — this is what closes Blocker 1).
3. Add the missing off-default mutation tests on all three routes (live, in-place-snapshot,
   relocated-snapshot) as kept vertical tests.
4. Resolve or explicitly shipping-gate R8 (qualifier-dropping rollup refusal), and add a
   collision test for R10 (same-named constraint threshold keys under one owner).
5. Execute retirement with **no provisional trim** (the 113-node deselection list must not be
   used in the final run).
6. Run the full licensed suites and real TEAx against the **actual retired tree** (not a
   simulation).
7. Audit that tree, then regenerate ONE internally consistent candidate record at the final
   paired OIDs (the current record has five internal discrepancies — see "Record integrity").

Also in scope from the review: fix the record-integrity discrepancies, and resolve the
spec-vs-baseline conflict on production ruff (16 pre-existing findings vs a spec that says
clean) — orchestrator recommendation was clear-the-16, NOT yet confirmed by owner.

## Context

### Where everything is

| Thing | Location | State |
|---|---|---|
| codegen rebuild worktree | `/home/reid/1cfe/sysml-codegen-item7-rebuild`, branch `item7-rebuild` | clean at `ceaade4` **[V]** |
| agentic-mbse rebuild worktree | `/home/reid/1cfe/agentic-mbse-item7-rebuild`, branch `item7-rebuild` | clean at `cc6c7a7` **[V]** |
| Original repos (DO NOT TOUCH refs) | `/home/reid/1cfe/sysml-codegen` @ `1672c57` on `source-identity-epic`; `/home/reid/1cfe/agentic-mbse` @ `5088b41` on `elaborate-first-salvage` | clean, unmoved **[V]** |
| Forensic branches (parts bin, never merge) | `item7-forensic-20260810`: codegen `07531e64`, agentic `ed5b8b02` | local, unpushed **[C]** |
| External byte archive | `/home/reid/1cfe/item7-recovery-archive/` (manifest SHA `26bdc230…`) | read-only evidence **[C]** |
| Pinned TEAx | `/home/reid/1cfe/teax` @ `fa0e06a9` | read-only **[C]** |
| Rebuild venv | `/home/reid/1cfe/item7-rebuild-venv` | wired to BOTH rebuild worktrees + teax-simkit **[V]** |

### The governing artifacts (read in this order)

1. **The plan** — `/home/reid/1cfe/sysml-codegen-item7-rebuild/.project/active/cutover-recovery/plan.md`.
   The single execution authority. Every orchestrator ruling is recorded inline with provenance
   markers ([OWNER …], [AGENT …]). The owner gate section (~line 1038) currently reads
   disposition PENDING — the REVISE verdict must be recorded there first.
2. **The owner's review** — in the session transcript; commit it verbatim (or near-verbatim)
   into `.project/active/cutover-recovery/` as the disposition record before any work.
3. **The ledger** — `ledger-4a.md` + `ledger-4a.json` (303 rows), checker
   `scripts/check_ledger_4a.py` (modes: paths / surface / groups / replacements), plus
   `scripts/check_proof_integrity.py` and `scripts/retirement_worklist.py` (`check`, `step N`).
4. **The retirement runbook** — in the plan (~line 4588): four steps, per-step row lists and
   edit tables, 55 prepared patches under
   `.project/active/cutover-recovery/runbook-patches/step{1,2}/` (steps 3–4 are deletions
   only), drift-checked by `tests/unit/test_runbook_patches.py`. The `PULLED_FORWARD` table
   lives in `scripts/retirement_worklist.py`. The 113-node trim the owner banned from the
   final run is `runbook-patches/provisional-trim.txt`.
5. **Evidence** — `evidence/`: `baseline.json` (Phase 2), `candidate.{md,json}` (stale in five
   named ways), `audit-3a..3e.md`, `audit-4.md`, `audit-5-final.md`,
   `dual-qualifier-drop-dryruns.txt`, `3e-package-comparison.md`, `phase5-runs/`.
6. **Briefs** — `briefs/phase*.md`: every stage's instructions; the trail of what each subagent
   was told. Follow the same pattern for new stages.
7. **The v6 batch** — `tests/fixtures/v6_recapture_batch/` + 15 per-fixture
   `instance_graph_snapshot.json` files, verified by `scripts/capture_v6_batch.py --verify`
   (15/22/0) and 58 nodes in `test_v6_recapture_batch.py`. Currently marked PROPOSED —
   step 1 of the revise path flips it to ACCEPTED (owner ruling given 2026-08-11).

### Orchestration mechanics (how this session ran; recommended to continue)

- Stages ran headless via `~/.claude/scripts/orchestrate-stage.sh run <stage>` /
  `resume <session_id>` with briefs on stdin. **Always pass `--perm bypassPermissions`
  explicitly on `resume`** — resume does not inherit the run-mode default and stages stall on
  permission walls without it **[V]**.
- Model: default (opus). Standing rule: never Fable for subagents **[C]** (auto-memory).
- Independent audit after every substantive implementation stage; findings fed back to the
  implementer session; ~2 rounds max, then orchestrator verifies objectively-checkable fixes
  directly.
- Commit per decision on `item7-rebuild`; briefs committed with their stages; OID-record
  commits after (a commit can't contain its own OID).

## The work, in recommended execution order

The owner's steps interact; this ordering avoids rework:

1. **Record keeping first (small, one commit):** owner disposition REVISE into the plan's owner
   gate; batch PROPOSED → ACCEPTED (README, manifest, plan, and the "authority for nothing"
   sentence in the Gate 4C part 4 notes); the owner's seven gated-item rulings written onto the
   gated entry; R8's orchestrator ruling amended (not appended) to the owner's overruling.
2. **The dual migrations (the big one).** Consumers migrate to the exact shapes; adapters/shims
   are banned by plan rule. Worklist = the measured probe inventories in
   `evidence/dual-qualifier-drop-dryruns.txt`: L-033/L-280 trio (expression compiler:
   `CompilationResult` name-keyed/mutable vs `ExactCompilationResult` UUID-keyed/frozen;
   `compile_calc_def` 4 params vs 1; 26 failed + 5 errors of 79), L-034 (58/86 nodes —
   name-keyed vs `_by_id`), L-036 (34/450, missing fields on identified payload), L-037
   (three duals: 134/450 on `usage_id`/`definition_id`, 134/450 on `expected_usage_ids`,
   4/450 different preflight arguments). **Two agentic production call sites have NO ledger
   row and WILL break if members are deleted without migration:**
   `src/agentic_mbse/validation/level4_constraints.py:55-56` and
   `level6_architecture.py:620-621` **[C — measured by probe this session]**. Coordinated
   paired commits (agentic first, codegen names the OID). This closes gated items 1–2 and
   unblocks L-033's deletion.
3. **The 111/113 test replacements (gated items 3–5).** The trim list
   (`provisional-trim.txt`): 59 extractor (L-135 — note one node needs `catf_mfe_model`,
   which the exact route refuses; its subject may need the `catf_mfe_d5` variant or a recorded
   per-node disposition), 47 hierarchy/AST (L-153 + L-100 — had NO disposition at all), 5
   compiler (L-281 — partially unblocked by the dual migration), 2 capture-filter (L-291/L-293
   area; owner gated item 6 also lands here). Standard: the part-6 bar (independent
   expectations, mechanism citations, no thinning; per-node dispositions only where a subject
   genuinely ends, with named replacement).
4. **Mutation matrix tests (owner step 3).** Extend
   `tests/execution/test_fusion_tea_mutation_teax.py` (currently live-generation only, line
   ~68) to generate from the in-place v6 snapshot and the relocated v6 snapshot, applying the
   C25 (availability 0.9→0.91 → LCOE `269.5300723203276` + Meier COE only) and C2 (thermal
   0.43→0.44 → LCOE `263.85170462810606` + f_recirc only) typed-entry injections on each.
   Owner independently ran relocated gain=100 successfully **[C]**, so expect certification
   work, not product work. Spec authority: `elaborator-cutover/spec.md:61`.
5. **R8 + R10 (owner step 4).** R8: owner overruled the accept-the-refusal ruling — preserve
   the resolved qualifier through rendering (the graph already distinguishes
   `panel.capital_cost` vs `caster.capital_cost`; only `elaborate.py:1937-1938`
   `input_name = fact.resolved_member_names[-1]` drops it), OR record a temporary product
   defect with an explicit shipping dependency on Item 10 — **owner has not yet chosen
   between fix-first-with-fallback and straight-to-shipping-gate; ASK before implementing**
   (open question 1). If fixing: check blast radius against the 3B option-C group-naming
   pins and the existing `SI_RENDERING_COLLISION` tests (`test_costed_component_exact_route.py:324`
   pins the refusal and must be inverted, not deleted). R10: add a collision test — two
   same-named constraint usages under one owner must not mint the same
   `…__threshold` key (or must refuse); today no corpus model has the shape **[C]**.
6. **Retirement, real, no trim (owner step 5).** Execute runbook steps 1–4 on the actual tree
   with the full battery per step boundary. Preconditions that must be green first: all of
   2–5 above landed (they empty the gated entry and make the trim unnecessary); the dead v5
   exports removal (gated item 7) happens inside steps 1–2 per the ledger rows.
   Order-critical facts already proven: G2′ (read path) must run before the family
   (graph_rebuild imports constraint_lowering); the two ledger/worklist patches must apply
   BEFORE `retire_step.py apply N`; patch exclusion by exact filename (substring drops a
   test patch); L-304 pulls forward to step 1. `retire_step.py close` closes each step in the
   ledger or the checker fails on state claims.
7. **Full gates on the retired tree + audit + one regenerated record (owner steps 6–7).**
   Full licensed suites both repos, corpus (15/22 exact multisets), real TEAx live+relocated
   at anchors, `--verify`, ruff/mypy, all checker modes, the 3E pins in their amended forms,
   v5 typed refusal still typed. Then an independent audit of the RETIRED tree (fresh
   session; use the phase5-audit brief as the template, scope adjusted). Then regenerate
   `candidate.{md,json}` from scratch at the final paired OIDs — fix the five discrepancies:
   candidate.md's "audit has not run" line and `c4e9b76` references, the 108-vs-112 commit
   count (108 = product tree through c4e9b76; recount at final OIDs), the owner-gate OID, and
   the Phase 4/5/audit progress checkboxes (plan ~line 156).
8. **Ruff decision:** spec requires clean production ruff; tree carries 16 pre-existing
   findings (inherited from Item 6, byte-identical throughout) **[V at ceaade4]**.
   Orchestrator recommended clearing the 16 during migration work; owner has not confirmed —
   open question 2. Do NOT silently amend the spec.

## Key Discoveries (gotchas the artifacts don't state loudly enough)

**Environment (will burn hours if ignored):**
- **The F2 trap, twice-bitten:** editable installs silently resolve to the WRONG worktree
  (cached wheels; or old-commit comparisons resolving to HEAD). ASSERT resolved `__file__` for
  `sysml_codegen`, `agentic_mbse`, AND `simkit` at every session start and after any venv
  operation; force PYTHONPATH for old-commit measurements **[V — a 4D stage misdiagnosed a
  "repo divergence" that was purely this]**.
- License: `set -a; source /home/reid/1cfe/agentic-mbse/.env; set +a` (no `.env` in codegen).
  The ONLY valid proof is zero `no live syside license` skip lines; counts don't discriminate
  **[V]**.
- Scratch worktrees for simulations go BESIDE the repos (`/home/reid/1cfe/`), never `/tmp` —
  five `test_ast_dispatch_invariant.py` failures are pure path resolution otherwise **[C]**.
- Some agentic tests shell out to bare `python`; host has only `python3` — put the venv's bin
  on PATH for acceptance runners **[C — caught in Phase 5 run 0]**.
- Expected clean numbers at `ceaade4`: codegen **3862 / 47 skipped / 53 deselected** licensed;
  agentic **1825 / 1 / 5**; execution lane **53** (incl. 12 real-TEAx nodes); corpus 15/22;
  ruff src 16 / tree 866→868 range; mypy 69 in 16 **[V]**.

**Process discoveries (validated the hard way):**
- **Scratch-worktree execution is the ONLY detector** for dynamic imports, pytest fixture
  requests, and transitive test-module breakage — the checker's five documented ceilings
  (`check_ledger_4a.py` docstring). Never trust `groups READY` alone; simulate **[V]**.
- **The patch drift check works:** editing any file a prepared patch touches breaks the patch;
  regenerate patches from a scratch worktree at the NEW committed HEAD (hand-editing hunk
  headers fails; this session tried) **[V]**.
- **The dual "just a rename" premise is false for all eight pairs** — measured, not argued.
  Rebind-and-run probes are the method; inventories are committed **[V]**.
- **`replacement_is_green` can be hollowed by deleting evidence:** 10 proof-node files
  depended on the v5 family; they were repointed to v6 evidence BEFORE retirement. If new
  proof nodes are authored, check what they depend on **[V]**.
- Corrections to recorded claims happen by AMENDING the record with the measurement (see the
  ruff 769-vs-301 arithmetic resolution in the plan) — never by appending a contradiction.

**Semantics settled this session (do not re-litigate; cite the plan):**
- B37-01: modeled aggregation executable ([OWNER pre-ruling], applied after evidence matched).
- Option-C group identity: stem-derived names, package-display fallback for `model.sysml`.
- C25/C2 mutation protocol: TEAx typed-entry injection; reseal-after-edit is refused by
  `check_reseal_provenance` and that refusal is part of the proof.
- Zero-constraint models: no synthetic aggregator module (named mechanism).
- The 22 corpus refusals are ratified semantics (`in R = R` rejection), not damage.
- Units annotations (`= X [unit]`): one rule, both spellings, fixed + pinned
  (`tests/fixtures/unit_annotation_lanes`).

## Suggested Skills

- `/_my_orchestrate` — continue running stages headless with briefs + audits; the pattern and
  its guardrails are proven across ~20 stages this session.
- `/_my_implement` — if executing a specific step directly instead of via orchestration.
- `/_my_audit` — after the dual migration and after the real retirement (owner path step 7
  requires auditing the retired tree).
- `/_my_recall` — auto-memory holds the environment traps (license, teax execution env,
  byte-identity churn) if this handoff's list needs corroboration.
- `/_my_wrap_up` — after the revise path lands, to persist the completion state.

## Open Questions (awaiting the owner)

1. **R8 approach:** fix-first (preserve qualifier through rendering, fall back to
   shipping-gate if blast radius exceeds the naming contract) vs straight-to-shipping-gate
   with Item 10 scheduled. Asked 2026-08-11; unanswered.
2. **Ruff 16:** clear the findings (recommended) or amend the spec. Asked; unanswered.
3. **Item 10 scheduling** (raised by R8 regardless of Q1's answer) — when does the cross-part
   qualifier work get scheduled? Not blocking the revise path.
4. Whether the owner wants the final retired-tree audit run by a fresh independent session
   (recommended, matches precedent) or folded into the regenerated candidate record's review.

## Non-negotiables that survive into this path (from the plan; unchanged by REVISE)

- Original branch refs never move; forensic branches never merge; archive read-only.
- No test deletion without a per-node recorded disposition + named replacement ("option (a)
  wholesale retirement" is refused permanently, on the record).
- Declared path sets before editing; unexplained changed path stops the stage.
- Rule-10 premise conflicts STOP for surfacing — the sessions that stopped were the ones that
  saved this recovery; brief every subagent accordingly.
- No pushes, tags, or promotion. Everything stays local until the owner says otherwise.
