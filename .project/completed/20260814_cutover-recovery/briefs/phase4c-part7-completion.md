# Stage brief — Phase 4, Gate 4C part 7 completion: the last 43 files

**You are completing the disposition pass** of the recovery plan:
`/home/reid/1cfe/sysml-codegen-item7-rebuild/.project/active/cutover-recovery/plan.md`.
Read first: the Phase 4 resequencing block (retirement is POST-ACCEPTANCE; nothing in this stage
deletes), the part-7 chunk records and the four named remaining pieces, `ledger-4a.md`/`.json`,
and the checker `groups`/`replacements` output at HEAD. Environment: the wired pair
(`item7-rebuild-venv` + `agentic-mbse-item7-rebuild`), imports asserted first; license
discipline as recorded.

## The four pieces (plan's own naming; verify counts at HEAD, don't inherit them)

1. **Execution lane** — L-192 + the three real-simkit siblings, L-113/L-114 behind them. This is
   the last Gate 4C must-restore (real constraint verdict execution on the exact route) and owns
   the live subject of the zero-constraint-aggregator mechanism. The specimens must execute real
   verdicts through the public exact route in the real-TEAx lane (both truth values,
   indeterminate, arithmetic-exception propagation, polarity — the responsibility list from the
   original forensics). Fixture: fusion_tea carries live constraints; use variants only if a
   shape is missing, at the established variant standard.
2. **L-180 `test_snapshot_v5_gate.py`** — repoint, never retire, and the repoint must hold
   BEFORE G2′ runs: the typed v5 refusal must be provable through a route that survives the read
   path's deletion. Pin it so the post-step-4 battery's "still typed" check has a subject.
3. **Expression compiler (L-284, L-281)** — blocked behind L-033's post-acceptance qualifier
   drop. Record the two-step disposition ON the rows (what the repoint will be, verified
   dry against the current dual so the post-acceptance step is mechanical); do not execute the
   rename now.
4. **The genuine repoints** — the remainder; coverage re-derived at the part-6 bar (independent
   expectations, mechanism citations, no thinning; import-localization preferred over
   whole-file retirement wherever one node carries the legacy touch).

## End state required

`groups` shows every deletion group blocked ONLY by the acceptance gate (zero undispositioned
files); `replacements` all green with the proof-integrity cross-check clean (no row proven by
evidence scheduled to break, including transitively through G2′/G3′/G4′ order); the retirement
runbook section in the plan updated so the post-acceptance execution is purely mechanical —
ordered steps, per-step row lists, per-step batteries, the L-180 precondition satisfied.

## Rules

Batteries per commit (suite delta-explained, corpus 15/22 via ledger test, `--verify` 15/22/0,
execution lane incl. real TEAx at anchors, ruff/mypy measured, `git diff --check`, checker all
modes). Nothing deletes; nothing thins; rule-10 stops as established. Coherent chunks if
multi-session; honest remainder if budget ends.

## Report back

Per-piece outcomes with node accounting, the end-state checker proof, the runbook state, OIDs.
`ARTIFACT:` the updated plan.
