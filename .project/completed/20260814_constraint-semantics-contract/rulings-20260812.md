# Constraint-semantics rulings — durable Q&A record (2026-08-12)

This is the durable capture of the eight-question decision session held 2026-08-12 between the
owner (Reid W) and the authoring agent, referenced by the spec's `(Q1)`–`(Q8)` labels. Each
ruling is **[AGENT] (ratified by owner, 2026-08-12)**: agent-proposed alternatives with stated
pros/cons, owner-selected. Per capture-fidelity, they remain agent-grade and are challengeable by
re-deriving against the recorded reasoning (full context:
`.project/research/20260812-101200_constraint-semantics-end-to-end.md` and the session
conversation). Owner-*stated* payloads are quoted verbatim and marked.

## Owner-stated frame and payloads (owner-originated)

- **[OWNER-VERBATIM, 2026-08-12]** "when we started this whole cleanup, it was while defining a
  policy around how to use constraints to enforce things like physics in a way that make our
  overall 'design search' viable"
- **[OWNER-VERBATIM, 2026-08-12]** "we know that narrow bands of viability may make design
  exploration really difficult. So I want to call out in our concept WHEN we really think
  equalities SHOULD be used (instructions) in addition to the sysml-codegen support"
- **[OWNER, 2026-08-12]** Required sequence: settle the semantics → fix documentation and the
  test model to match (capturing expected output) → then run tests to confirm.

Attestation (authoring session, 2026-08-12): both verbatim quotes reproduce the owner's session
messages exactly (spec-review L1-4).

## The eight rulings

| # | Question | Alternatives presented | Owner selection |
|---|---|---|---|
| Q1 | What makes a constraint binding (an enforced gate)? | (a) assert-only, standard-aligned; (b) plain constraints gate too; (c) three-tier: assert=gate, plain-with-executable-predicate=evaluated advisory | **(a) Assert-only**; advisory tier recorded as possible future extension only |
| Q2 | Ruling for calc-def-owned constraints (51 structurally unreachable CATF guards)? | (a) rule per-occurrence semantics + build now; (b) rule semantics + staged delivery as its own designed item; (c) part-level only, permanent | **(b) Rule + stage**: asserted calc-def constraints mean one check per calculation occurrence and will execute; capability staged; interim = visible disposition + asserted-unattachable is a loud error |
| Q3 | How loud is an asserted constraint with zero concrete checks? (totality itself pre-agreed: every swept usage gets exactly one disposition, hard-gated) | (a) severity by cause; (b) always error, vacuous included; (c) never error, visibility only | **(a) Severity by cause**: asserted+structurally-unattachable = generation error; asserted+vacuous = warning-grade visible disposition + authoring advisory; plain/out-of-scope forms = records, never errors |
| Q4 | What may a gate's predicate contain? | (a) bindings-only + library, permanent; (b) admit in-predicate chains now; (c) bindings-only now, chains filed as future capability; (d) first-class tolerance `==` | **(c) Bindings-only now, chain support filed as future capability**; equalities = two-inequality tolerance bands + reusable constraint-def library; owner additionally flagged the equality-usage instruction (verbatim above) |
| Q5 | Where does coverage live; what may `all_satisfied` mean? | (a) in the headline (all_satisfied = full coverage; new partial token; report embeds counts; catalog keeps detail); (b) separate coverage field, mandatory join; (c) counts only | **(a) In the headline** with two-tier accounting (usages for coverage, occurrences for results); vacuous bucket separate |
| Q6 | Study-policy default for the partial-coverage headline? | (a) keep-for-boundary + explicit auditable per-study opt-in to feed-strategy; (b) feed-strategy default; (c) penalize default; (d) reject | **(a) Boundary + opt-in**; coverage lands in durable case records |
| Q7 | Requirements-side forms (`require`/`assume`/`satisfy`)? | presented as settled-unless-objection: stay non-executable and visible; `satisfy` is an assertion per the standard but requirement evaluation is a declared out-of-scope capability (named exclusion, not the unreachable-assert error) | **No objection — adopted as presented** |
| Q8 | Where does the CATF constraint migration happen? | (a) new derivative fixture forked from catf_mfe_d5, twins frozen; (b) migrate twins in place; (c) fresh purpose-built model | **(a) New derivative**; per-constraint disposition table with owner tolerance sign-off; `[m]`-literal defect + chain diagnostic = in-scope must-fixes |

## Post-ruling refinements

Recorded here as they are ratified; the spec carries the requirement text. All four below are
**[AGENT] (ratified by owner, 2026-08-12)**, selected from presented alternatives in the same
recorded session (spec-review round):

- **L2-1 — Two totals.** Inventory totality (catalog, every authored usage) is separate from
  feasibility coverage, whose denominator is applicable **asserted** gates only. Descriptive and
  requirement-side usages never enter the feasibility denominator. (Alternative rejected: all
  authored usages in the denominator.)
- **L2-2 — Vacuous asserted gates count as missing assessment by default** (supersedes the Q5
  "vacuous doesn't block full" detail): partial coverage until the model is fixed or an explicit
  inapplicability disposition is recorded; mechanism deferred to design. (Alternative rejected:
  warning-only with full coverage unaffected.)
- **L3-1/L3-3 — All-65 disposition table** in the CATF derivative migration, with the owner
  tolerance checkpoint hosted in the CATF child item before its design. (Alternative rejected:
  nine-only scope.)
- **L2-3 — Umbrella + child items** via `/_my_epic_plan`; this spec stays the behavioral
  authority. (Alternative rejected: single cross-repo implementation item.)
