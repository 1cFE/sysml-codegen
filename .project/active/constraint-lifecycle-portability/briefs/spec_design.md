# Combined Spec+Design Brief — Lifecycle Item 5: Whole-Tree Snapshot Portability

**Stage:** spec (producing a combined spec+design artifact — this item's risk is a bounded
sweep, not new architecture; one document, one review pass at audit)
**Epic authority:** Item 5 (register row 5) in
`.project/backlog/epic_constraint_execution_lifecycle_remediation.md`; ratified contract row 5.

## Intent

- [INHERITED: ratified contract] Extend the certified bounded portability (superseded-epic
  Item 4, inherited — do NOT re-audit it) to EVERY semantic and generated byte: loader-
  reconstructed fields, eligible/excluded IDs, fingerprints, catalogs/contracts/reports,
  generated code and calculation docstrings, the full output tree.
- Same semantic input at two checkout roots → byte-identical output and identity. No
  same-machine path cancellation (two real roots + an absolute-byte scan over the whole tree).
- Completes Item 1's deferred relocated anonymous leg: the anonymous-admitted-with-actual
  fixture must pass the relocated route (Item 1 evidence names it open for exactly this item).
- Preserve source referent meaning; reuse the certified transaction/shape-gate machinery.
- [OWNER] No LOC metrics. Qualitative simplicity: consolidate/delete obsolete
  path-normalization branches rather than adding another normalization layer.

## Current facts to ground in (verify at HEAD `1ce3247` / agentic-mbse `4c18d61`)

- Snapshot format is v4 (amended in place this branch, never shipped); the licensed re-capture
  procedure and captured_at byte-identity discipline are fresh from Item 4 — reuse, don't
  reinvent.
- Items 1–4 landed heavy churn in the resolution/lowering/enrichment paths; the portability
  sweep must inventory absolute-path producers at TODAY's code, not the pre-epic map.
- The stale-baseline class (deep_cross_scope, plant_values, constraint_inline,
  dropped_constraints) is recorded and unowned — if this item's two-root proof trips over one,
  handle per the recorded pattern (parent-commit reproduction), do not absorb silently.
- Known relocated-vs-live trap from prior epics: multi-hop EXPOSE re-tag (verify channel
  identity, not just fallback removal — project memory `multihop-expose-offline-parity`).

## Out of scope (firewall)

Snapshot schema expansion unrelated to portable referents; historical-snapshot reproducibility
under historical profiles; Item 7's seal/provenance work; re-auditing the certified bounded
manifest.

## Artifact shape

One document: provenance-graded requirements, the absolute-byte inventory (measured, not
assumed — scan the actual generated tree for checkout-root bytes at two roots), design of the
minimal normalization (delete-over-add), acceptance coordinates (live A / live B / relocated
replay; the Item 1 anonymous leg named explicitly), phased plan with commands and stop
conditions, deletion inventory.
