# Adjacent-Work Register (SOURCE-IDENTITY Item 2)

**Date**: 2026-08-05 · **Branch**: `nested-override-tripwire` @ `fa9e0d0`

One owner and one disposition per overlapping mechanism, per the Item-2 success
criterion: no second occurrence→definition bridge or part-structure authority is
proposed anywhere below.

| # | Mechanism | Evidence from this spike | Disposition | Owner |
|---|---|---|---|---|
| 1 | Lifecycle Item-10 per-child `:>>` capture + transitive instance routing (landed) | Not contradicted; the VBR tier-2 specialized-chain path is distinct from the tier-1 fan-out stamp | **Reuse as substrate; audit, don't re-implement** | SOURCE-IDENTITY Item 4 |
| 2 | `[NESTED-OCCURRENCE-OVERRIDE]` (BACKLOG; absorbed into Item 4, 2026-08-04) | Overlap **revalidated as structural, not incidental**: Path B's resolution miss is the same def-keyed-index vs occurrence-keyed-demand gap the supplied-value materializer has. Item 1 adds that SysIDE exposes the bridge directly (`owned_redefinitions` → `.redefined_feature`/`.redefining_feature`). One bridge serves binding resolution and value materialization. | **Absorption confirmed**; tripwire + probe fixture are acceptance evidence. Note: the probe fixture ships without a snapshot (capture halts by design) — Item 4 acceptance needs its live leg. | SOURCE-IDENTITY Item 4 |
| 3 | `[CONSTRAINT-ARCH-UNIFY]` sub-scope 2 (one part-structure index) | The three concrete-instance walkers are still present; the Item-4 bridge needs only occurrence-type threading into `enrich_graph_design_attributes` | **Sequencing stands as filed** (BACKLOG note, 2026-08-04): Item 4 threads the *existing* occurrence index, builds no second walker; the consolidation remainder beyond source identity stays in CONSTRAINT-ARCH-UNIFY | Item 4 (source-identity portion) / CONSTRAINT-ARCH-UNIFY (remainder) |
| 4 | Queued fusion-tea producer-channel/aggregation-scoping finding (unfiled; owner filing gate) | Aggregation terms share the lenient terminal-mint behavior (solar `permitting.*` I7 per-term mints — route-matrix row 13): same semantic family, aggregation consumer type | **Recommend absorption into this epic's matrix** (Stage-2 disposition per the epic's Dependencies section); the filing itself remains the owner's call | Owner, at the Item-3 checkpoint |
| 5 | Parameter-group-deriver value backfill (`src/sysml_codegen/resolution/graph_builder.py:620-630`) | **Newly identified this spike** — a fourth value-resolution authority that attaches def-default values to per-consumer EPs, masking Path-B identity loss; in no backlog entry before now | **Add to Item 5's superseded-route deletion register**: after the cutover, either it derives from the single identity authority or it is deleted | SOURCE-IDENTITY Item 5 |
| 6 | SVM occurrence-attribute synthesis (`enrich_graph_design_attributes`) | Solar's converged `pack_count` field exists *only* as a synthesized attribute (absent from the raw captured map) — synthesis currently manufactures occurrence identity on its own authority | **Item 4/5 must make synthesis derive from the one semantic identity**, not run as a parallel authority; do not delete it before the cutover (it carries the constraint route's convergence today) | SOURCE-IDENTITY Items 4/5 |
| 7 | `#(i)` index-segment drop in `_parse_chain_expression` (found by Item 1, form 4a) | Stage-0 identity-loss site; zero authored instances in any corpus | **Fold into Item 3's authoring-form disposition** (reject/defer is free today); if the form is ever supported, extraction must carry the index into identity | SOURCE-IDENTITY Item 3 (disposition), Item 4 (if supported) |
