# GAP-CLOSE Item 1 Implementation Evidence

**Scope:** sysml-codegen only. `[GAP-CLOSE-F1-TEAX-NORMALIZATION]` remains external and open.

This record is filled from the exact commands and preserved outputs during implementation. F2 RED
records are separate from the already-green F1 characterization.

## Frozen Inputs

- Baseline revision: `6db321225a5c8568db0287b67ed1d04c03079cc2`.
- Detached roots: `/tmp/gap-runtime-contract-evidence.qehvkM/{pre,post}`.
- Final frozen overlay SHA-256:
  `b85fb3c9dfa526ff4a60cebea6bfff7b4940d454dd04a37e7764350f3cea7606`.
- Final runner SHA-256: `ce5456fb668b557b3262d0f448dea263a062d7d88963416cdb6874c91933064d`.
- Final classifier SHA-256:
  `c762e64f481d11b089eeba4e2cacb4cec1f2e421570ab54b23ae1c26c500e8fd`.
- Production patch SHA-256:
  `dc3547b17366fc894a5b60a59df198b692020aafbd86221515011c3f6f84ea9b` after the pre-PR
  trailing-whitespace cleanup. Applying that normalized transport patch regenerates the original
  three-file binary diff with SHA-256
  `557282350ac727fb207a56668868529090cf2d390c09bca2e82b63ad42c9cb93`.
- Shared `plant_values` input manifest:
  `5ce74869c17d8f5699970d7494c23fa585fc2ad5698e6ad2ca6d38dfe1bdefb5`.
- Environment: Python 3.12 through the existing agentic-mbse uv environment, worktree-first
  `PYTHONPATH`, `PYTHONNOUSERSITE=1`, and `PYTHONDONTWRITEBYTECODE=1`. The overlay asserts HEAD and
  both imported source paths before collecting behavior.

## F2 Pre-Fix RED

The exact Appendix A2 command was run separately for each stable node and once for the group.

- `case-fold`: exit 1; one failure; `DID NOT RAISE CodeGenerationError` for raw keys
  `Pkg::Foo` and `Pkg::foo`.
- `underscore-run`: exit 1; one failure; `DID NOT RAISE CodeGenerationError` for raw keys
  `Pkg::foo__bar` and `Pkg::foo_bar`.
- `quoted-hyphen`: exit 1; one failure; `DID NOT RAISE CodeGenerationError` for raw keys
  `Pkg::'Foo-Bar'` and `Pkg::Foo_Bar`.
- Combined: exit 1; exactly three failures, all for the same missing rejection. There were no
  collection, import, revision, or source-selection failures.
- Old-impact node: passed. It proved both raw keys emitted `constraint_pred_pkg__foo`; executing the
  later source replaced the earlier body and changed the reachable result from true to false.

## F1 Baseline Characterization

The unchanged overlay's four F1 nodes passed at the baseline: float division by zero,
`0.0 ** -1.0`, exponent overflow, and division by zero nested under `and`. The assertions pin the
original Python exception class and exact message. These are characterization, not RED evidence.

## Candidate and Validation

## Candidate GREEN and No-Write

- Final refreshed overlay: F2 + F1 + both no-write nodes reported **9 passed** in the isolated
  candidate. The candidate process asserted baseline HEAD, worktree-local imports, and the exact
  production diff hash before behavior.
- Kept direct/CLI tests: **31 passed** for emission and no-write behavior. Adjacent CLI safety:
  **18 passed**. Final focused unit set: **69 passed**.
- Absent target stayed absent. The populated tree retained the same complete manifest of path
  kinds, symlink target, and regular-file bytes.

## Generated Bytes and Routes

- Baseline versus candidate snapshot generation used one copied, hashed `plant_values` input.
  The strict classifier found only the approved wrapper sentence and its derived
  `contracts/package_contract.json`; `unapproved: []`.
- `modules/constraints/predicates.py`, emitted function names, and wrapper predicate import lines
  were identical. No fixture byte changed.
- Licensed live generation completed in the agentic-mbse environment. The strict complete-tree
  live/snapshot classifier reported `changed: []` and `unapproved: []` before behavior execution.
- Fresh subprocesses ran finite and non-finite cases for both live and snapshot trees. All four
  completed; finite evidence remained satisfied/violated, while non-finite evidence remained
  indeterminate. Each process asserted its generated package and codegen roots first.

## Project Gates

- Predicate compiler: **38 passed**. Generated execution lane (raising wrapper, finite/violated,
  non-finite/indeterminate): **3 passed**.
- Selected snapshot conformance: **1 passed, 1 skipped** in the default project environment; the
  separately invoked licensed live/snapshot gate passed.
- Full default-environment suite: **2,169 passed, 205 skipped, 9 deselected, 23 failed, 96 errors**.
  Every failure/error reported missing SysIDE license setup in that environment. No focused or
  license-free regression failed.
- Ruff passed for `src/`, every touched test, and all three evidence scripts. Eight touched/new
  Python files pass `ruff format --check`. The CLI file has unchanged inherited whole-file format
  debt: both baseline and candidate report `Would reformat`; retaining only the approved preflight
  hunk avoids 148 lines of unrelated formatting churn.
- Full and targeted mypy both reproduce the recorded baseline exactly: **76 errors in 17 files**;
  no new error points at the new helper, validator, or preflight.
- `git diff --check` passed. `git diff -- tests/fixtures` and fixture status were empty. Scope review
  found only the three approved production files, four kept test files, and project/evidence
  artifacts; no TEAx, sanitizer, arithmetic compiler, snapshot format, or metadata edit exists.

## External Dependency

`[GAP-CLOSE-F1-TEAX-NORMALIZATION]` remains an open P0 outside this repository. This evidence
characterizes original exception propagation only. It does not establish `EvaluationFailed`,
module identity, both-evaluator parity, or end-to-end F1 closure.
